apiVersion: v1
kind: Secret
metadata:
  name: gcp-credentials
  labels:
    app: gcp-credentials
type: Opaque
data:
  GCP_STORAGE_BUCKET_NAME: base64 encoded value
  gcp-service-key.json: base64 encoded file
